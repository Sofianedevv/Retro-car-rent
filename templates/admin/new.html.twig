{% extends 'admin/base.html.twig' %}

{% block admin_content %}
    <div class="vehicle-form">
        <h1>{% if vehicle_type == 'car' %}Ajouter une voiture{% endif %}</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Enregistrer</button>
        {{ form_end(form) }}
    </div>
{% endblock %} 