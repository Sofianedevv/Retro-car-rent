<footer class="bg-gray-900 text-white py-8 font-playfair">
    <div class="container mx-auto px-4 grid grid-cols-1 md:grid-cols-3 gap-8">

        <!-- Logo et Description -->
        <div class="flex flex-col items-center md:items-start">
            <a href="{{ path('app') }}" class="flex items-center mb-4">
                <img src="{{ asset('template-html/assets/images/logo.svg') }}" alt="Logo" class="h-16 w-auto">
            </a>
            <p class="text-gray-400 text-center md:text-left">
                Location de voitures vintage et rétro authentiques. Élégance et charme pour toutes vos occasions !
            </p>
        </div>

        <!-- Navigation -->
        <div class="flex flex-col items-center md:items-start">
            <h3 class="text-lg font-semibold mb-3">Navigation</h3>
            <ul class="space-y-2">
                <li><a href="{{ path('app') }}" class="hover:text-gray-300 transition duration-300">Accueil</a></li>
                <li><a href="{{ path('app_collections') }}" class="hover:text-gray-300 transition duration-300">Véhicules</a></li>
                <li><a href="{{ path('app_contact') }}" class="hover:text-gray-300 transition duration-300">Contact</a></li>
                {% if app.user %}
                    <li><a href="{{ path('app_profile') }}" class="hover:text-gray-300 transition duration-300">Mon Profil</a></li>
                {% else %}
                    <li><a href="{{ path('app_login') }}" class="hover:text-gray-300 transition duration-300">Connexion</a></li>
                {% endif %}
            </ul>
        </div>

        <!-- Contact & Réseaux sociaux -->
        <div class="flex flex-col items-center md:items-start">
            <h3 class="text-lg font-semibold mb-3">Contact</h3>
            <p class="text-gray-400">Email : contact@retro-car-rent.com</p>
            <p class="text-gray-400">Téléphone : +33 (0)1 23 45 67 89</p>

            <div class="flex space-x-4 mt-4">
                <a href="#" class="text-gray-400 hover:text-white transition duration-300">
                    <i class="fab fa-facebook text-2xl"></i>
                </a>
                <a href="#" class="text-gray-400 hover:text-white transition duration-300">
                    <i class="fab fa-instagram text-2xl"></i>
                </a>
                <a href="#" class="text-gray-400 hover:text-white transition duration-300">
                    <i class="fab fa-twitter text-2xl"></i>
                </a>
            </div>
        </div>

    </div>

    <!-- Copyright -->
    <div class="border-t border-gray-700 mt-6 pt-4 text-center text-gray-400 text-sm">
        &copy; {{ "now"|date("Y") }} Retro Car Rent - Tous droits réservés.
    </div>
</footer>
