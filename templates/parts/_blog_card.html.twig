<div class="flex flex-col p-6 bg-[#faf5e4] rounded-lg shadow-lg shadow-[#b3a97d] transition-transform duration-300 hover:scale-105">
    <img class="object-cover object-center w-full h-auto mb-8 rounded-lg" src="{{ image_url }}" alt="blog">
    <h1 class="mb-4 text-xl font-semibold leading-none tracking-tighter text-gray-900 lg:text-2xl">{{ title | default('Default Title') }}</h1>
    <p class="flex-grow text-base font-medium leading-relaxed text-gray-800">{{ desc | default('Default Description') }}</p>
    <div class="mt-8">
    </div>
</div>