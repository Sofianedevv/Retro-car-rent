<div class="bg-white p-4 rounded-lg shadow">
    <img src={{car.defaultImage}} alt={{car.model}}
         class="w-full object-cover mb-4 rounded-lg">
    <a href="#" class="text-lg font-semibold mb-2">{{car.model}}</a>
    <p class=" my-2">{{car.brand}}</p>
    <div class="flex items-center mb-4">
        <span class="text-lg font-bold text-primary">{{car.price}}</span>
    </div>

    <button class="bg-primary border border-transparent hover:bg-transparent hover:border-primary text-white hover:text-primary font-semibold py-2 px-4 rounded-full w-full">
        Add to Cart
    </button>


</div>
