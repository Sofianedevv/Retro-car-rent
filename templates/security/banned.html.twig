{% extends 'base.html.twig' %}

{% block title %}Compte banni{% endblock %}

{% block content %}
<div class="min-h-screen flex items-center justify-center bg-[#F5F5F0] py-12">
    <div class="max-w-md w-full bg-white p-8 rounded-lg shadow-2xl border-2 border-red-500">
        <div class="text-center mb-8">
            <div class="text-red-500 mb-4">
                <i class="fas fa-ban text-6xl"></i>
            </div>
            <h1 class="text-4xl font-playfair font-bold text-red-700">Compte banni</h1>
            <div class="mt-4 text-gray-600">
                <p class="mb-4">Votre compte a été suspendu par un administrateur.</p>
                <p class="mb-4">Si vous pensez qu'il s'agit d'une erreur ou si vous souhaitez faire appel de cette décision, veuillez nous contacter.</p>
                <p class="text-sm text-gray-500">Email de contact : contact@retro-car-rent.com</p>
            </div>
        </div>

        <div class="mt-6 text-center">
            <a href="{{ path('app_logout') }}" 
               class="inline-block px-6 py-3 bg-gray-600 text-white rounded-lg hover:bg-gray-700 transition-colors">
                Retour à l'accueil
            </a>
        </div>
    </div>
</div>
{% endblock %} 